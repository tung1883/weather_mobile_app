import { StyleSheet } from "react-native";

export const lightStyles = StyleSheet.create({
    bgColor: "#FFFFFF",
    secondaryBgColor: "#D3D3D3",
    contentColor: "black",
});

export const darkStyles = StyleSheet.create({
    bgColor: "#1E1E1E",
    secondaryBgColor: "#696969",
    contentColor: "white",
});
